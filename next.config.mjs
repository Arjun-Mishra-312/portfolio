/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
  images: {
    domains: ['aceternity.com','images.unsplash.com'], // Add the hostname here
  },

};

export default nextConfig;
